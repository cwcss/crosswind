.cache
.DS_Store
.idea
*.log

# Secrets. Nothing here was ignored before, so a stray `.env` holding a
# publish token would have been committable to a public repo.
.env
.env.*
!.env.example
*.tgz
coverage
dist
lib-cov
logs
node_modules
temp
# Compiled CLI binaries and their release archives — build artifacts, and the
# standalone binary is ~64MB, which is far past what belongs in a git object.
packages/ts-css/bin/cssx
packages/ts-css/bin/cssx-*
packages/ts-css/bin/*.zip
pantry
